name: TDD 測試驅動
description: 用於提交測試驅動開發的測試計畫，包含測試矩陣與場景驗證。
labels: ["type: testing", "domain: tdd"]
assignees: []
title: "TDD: [測試名稱]"
body:
  - type: markdown
    attributes:
      value: |
        ## TDD 測試計畫表單
        請填寫以下資訊，建立完整的測試矩陣與驗證策略

  - type: input
    id: bdd-issue
    attributes:
      label: 對應 BDD Issue
      description: 請輸入此 TDD 對應的 BDD Issue 編號（格式為 #123）。
      placeholder: "#123"
    validations:
      required: true

  - type: input
    id: sdd-issue
    attributes:
      label: 對應 SDD Issue
      description: 請輸入此 TDD 參考的 SDD Issue 編號（格式為 #123），作為設計規範。
      placeholder: "#123"
    validations:
      required: true

  - type: textarea
    id: test-matrix
    attributes:
      label: 測試矩陣
      description: |
        建立測試矩陣表格，每行代表一個測試案例，含 Test ID、Scenario ID (BDD)、測試類型、優先順序、資料準備方式、預期結果等。
        使用 checkbox (- [ ]) 來標記實現進度。
      placeholder: |
        | Test ID | Scenario ID (BDD) | 測試類型 | 優先順序 | 資料/Mock | 預期結果 | Red | Green | Refactor | 備註 |
        | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
        | T-101 | US1-S1 | E2E | P0 | 真實環境 | 系統成功響應 | ⏳ | ⏳ | ⏳ | 成功路徑 |
        | T-102 | US1-S1 | E2E | P0 | Mock 失敗 | 系統返回錯誤訊息 | ⏳ | ⏳ | ⏳ | 失敗路徑 |
        | T-103 | US1-S1 | Unit | P1 | 單元測試資料 | 驗證邏輯正確 | ⏳ | ⏳ | ⏳ | 邊界情況 |
        
        **測試狀態說明**：
        - `⏳` = 未開始 | `🔴` = Red（測試失敗） | `🟢` = Green（測試通過） | `♻️` = Refactor（已優化）

  - type: textarea
    id: test-scenarios
    attributes:
      label: 測試場景
      description: |
        列出各個測試場景的詳細說明，包括場景名稱、輸入、預期結果。每項使用 checkbox (- [ ]) 標記完成進度。
      placeholder: |
        - [ ] **T-101: 成功建立 Issue**
          - Scenario ID: US1-S1
          - 輸入：完整表單資料
          - 預期：Issue 建立成功，返回編號

        - [ ] **T-102: 欄位驗證失敗**
          - Scenario ID: US1-S1
          - 輸入：缺漏必填欄位
          - 預期：系統提示補充，驗證通過後建立

  - type: textarea
    id: test-data
    attributes:
      label: 測試資料準備
      description: |
        說明測試資料策略、Mock 資料來源、真實資料準備方式、邊界資料等。
      placeholder: |
        - **Mock 資料**：使用 Jest Mock fixtures 模擬 API 回應
        - **真實資料**：使用測試資料庫中的預設資料集
        - **邊界資料**：空值、極限值、特殊字元等
        - **資料準備**：於 setup.js 中初始化，teardown.js 中清理

  - type: textarea
    id: expected-results
    attributes:
      label: 預期結果與通過條件
      description: |
        定義各測試場景的通過條件，使用 checkbox (- [ ]) 標記驗收進度。
      placeholder: |
        - [ ] **T-101 通過條件**：Issue 建立成功，標籤與欄位正確
        - [ ] **T-102 通過條件**：驗證邏輯正確，提示訊息清楚
        - [ ] **整體通過條件**：所有測試場景通過，覆蓋率 ≥ 80%


